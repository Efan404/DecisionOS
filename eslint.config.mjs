// ESLint flat config placeholder.
// eslint-config-next does not support ESLint 9 flat config until Next.js 15.
// Linting is run separately via `next lint` (which uses its own internal config).
/** @type {import('eslint').Linter.Config[]} */
export default []
